// What goes in here  
// schema and validation from items entering database

const mongoose = require('mongoose');
const Schema = mongoose.Schema; 

const Index = new Schema({

});

module.exports = mongoose.model('Index', Index); 